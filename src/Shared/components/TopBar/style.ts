import { styled, Grid2 } from "@mui/material";

const TopBarContainer = styled(Grid2)({
  width: "100%",
  height: "20px",
  backgroundColor: `var(--color-blue-primary)`,
});

export default TopBarContainer;
